# Internal Bug Tracker

This document tracks internal bugs, regression, and known issues within the ProjectWAR emulator.

## Active Bugs

| ID | Summary | Description | Status | Priority | Reported |
|:---|:---|:---|:---|:---|:---|
| BUG-001 | Invader items not awarding Superior Ward fragments | Resolved 2026-08-30. This was the narrow Tome-fragment defect, not the complete ward system. The equip path and `TokUnlock3` column existed, but every row in that column was zero. The client mapping identifies boots 7610, gloves 7611, shoulders 7612, helm 7613, and chest 7614. `05_restore_invader_superior_ward_unlocks.sql` restores those mappings for all 24 Invader sets (120 items); belts intentionally grant no fragment. | Resolved | Medium | 2026-03-30 |
| BUG-002 | Floating chest objects | Ruinous Powers used the official chest Z at a different X/Y position almost forty world units away on sloped terrain. Script `12` moves that PQ's reward chest to the complete capture-verified `(858858, 838868, 6344)` position. This fixes the reported case without guessing at a generic terrain offset; other floating chests still require evidence per location. | In Progress | Low | 2026-03-30 |
| BUG-003 | RvR zone transition crash | Transitioning from Reikland to Reikwald crashes the server. Two root causes found and fixed 2026-08-29, both on the zone-transition path: (1) `RegionMgr.CheckZone` dereferenced `obj.Zone.Info` although `Object.Zone` is documented nullable and is null between zones — thrown from `SetOffset` on the movement path, outside the region tick's exception handler, so it terminated the process; (2) `RegionMgr.ZonesMgr` was a plain `List<ZoneMgr>` mutated by on-demand zone creation while `WorldMgr.GetZonesFightLevel` enumerated it from the world thread. Entering Reikwald for the first time creates its `ZoneMgr`, hitting both. Now a lock-guarded find-or-create plus a `GetZones()` snapshot for readers. Awaiting in-client reproduction to close. | In Progress | High | 2026-03-30 |
| BUG-004 | LOS vertex position precision error | ~16-unit max Y error from NIF world-matrix accumulation during native LOS generation. | Open | Low | 2026-03-30 |
| BUG-005 | LOS missing water generation | Zone 280 (and others) missing `water.xml` in current extracted data, leading to missing water chunks in generated LOS. | Open | Low | 2026-03-30 |
| BUG-006 | LOS multi-zone coverage gap | Native LOS generation currently only fully supported for zone 280 due to missing source files for other zones in current extraction. | Open | Medium | 2026-03-30 |
| BUG-007 | Ability data gaps | 12,664 abilities identified with Partial or StringsOnly coverage in `ClientDataMatrix` analysis. | Open | Low | 2026-03-30 |
| BUG-009 | Orphaned `creature_abilities` rows | 22 rows in `war_world.creature_abilities` reference ability ids that `AbilityMgr.GetAbilityInfo` cannot resolve, so those NPCs silently load without the ability (`AbilityMgr.cs:887`, DEBUG only). Concentrated in a few ids — `7843` accounts for 12 references, plus `3384`, `3375`, `5054`, `5057`, `5063`, `10811`, and `10812`. `06_remove_invalid_creature_ability_header.sql` removes the twenty-third record, a CSV header imported as a row with both IDs zero. Resolving the remaining rows needs real ability definitions from a source of truth, not invented data. | Open | Low | 2026-08-30 |
| BUG-008 | Unknown server-side operations | Component operations 29, 30, 32, 40, 41, 47, 51 remain unnamed and their semantics are opaque. | Open | Low | 2026-03-30 |

| BUG-010 | Five taxi destinations land outside their own zone | `zone_taxis` rows for zones 62, 132, 139, 168 and 204 hold coordinates that fall outside the zone they name. `MovementHandlers.cs:949` teleports to `(ZoneID, WorldX, WorldY)` as a unit, so taking one of these flights would put the player outside the world geometry. Four rows (132, 139, 168 and 204) were already disabled in the database; only zone 62 Karaz-a-Karak was exposed. As of 2026-08-30 `ZoneService.NormalizeTaxiWorldPosition` disables any taxi it cannot normalize, so the remaining bad flight is hidden rather than offered. The extracted client, toolkit database, and 2012 emulator dump contain no replacement taxi records, and none of these five zones has a flight-master spawn in the current database. Closing this needs correct coordinates from a source of truth; do not invent them. | In Progress | Medium | 2026-08-30 |
| BUG-011 | `RegionMgr.Players` read across threads | `Players` is documented "should only be accessed from within this region's thread" and is mutated unsynchronized by `AddNewObjects`/`RemoveOldObjects` on the region thread. `InstanceMgr` enumerates it from GM commands on the packet-handler thread (`InstanceMgr.cs:243`) and `Instance.CheckInstanceEmpty` reads `.Count` from the instance thread. Same class as the BUG-003 crash, but contained: packet dispatch wraps handlers in try/catch, so the symptom is a GM command that intermittently fails rather than a server crash. A real fix needs either a lock in the region tick or a copy-on-write list; both touch a hot path used in ~15 places, so this is deliberately deferred. | Open | Low | 2026-08-30 |
| BUG-013 | Keeps have no guild claim objective | The 2019 guild-claim code added its columns and lookup without database rows. Original live-server packet captures in WAR-RE-Toolkit yielded exact named flag packets for 21 of 24 active non-fortress keeps; `04_restore_guild_keep_claim_flags.sql` restores only those proven rows. The handler now enforces the guild `CLAIM_KEEP` permission, attacking realm, and completion-time revalidation. Gnol Baraz, Thickmuck Pit, and Stoneclaw Castle remain deliberately unmapped until authoritative packets or coordinates are found. | In Progress | Medium | 2026-08-30 |
| BUG-015 | Clean shutdown never ran | `Program.OnClose` was wired only to `Console.CancelKeyPress`, but `ServerLauncher` stopped services with `Process.Kill()` — TerminateProcess, which delivers no signal. Across nine sessions on 2026-08-30 the string "Closing the server" appeared zero times, so `SaveRuntimeState()`, `ForceSave()` on both databases and `Player.Quit()` had never executed on the supported launch path. Exposure was bounded by the 60s save pump, the per-player autosave and `Campaign.cs:735` persisting progression on battlefront transitions, so this cost campaign state and up to a pump interval of writes rather than whole characters. Fixed 2026-08-30: shutdown is now idempotent (`Interlocked` guard), reachable from `SetConsoleCtrlHandler` (Ctrl+C, Ctrl+Break, window close, logoff, shutdown) and `ProcessExit`, runs each step under its own try/catch, and flushes NLog last; the launcher closes rather than kills, with `Kill()` only after a 15s timeout. Verified: CTRL_CLOSE now logs `Closing the server (console control event 2)`, completes in 0.27s and leaves `realms.OnlinePlayers = 0`. | Resolved | High | 2026-08-30 |
| BUG-016 | Logging configuration unsafe for a live server | Four defects in `WorldServer/NLog.config`, all fixed 2026-08-30. (1) NLog's own internal log ran at Trace into a hardcoded `c:\temp\nlog-internal.log` with no rotation, two to four lines per log event, and had reached 656 MB since 2026-03-20 — now `Off`. (2) The async wrapper used `overflowAction="Block"`, so a full queue stalls the producing thread; on the combat path that is the 50 ms region tick waiting on disk — now `Discard` with an explicit 20,000 queue limit. (3) The catch-all rules ran at Trace, producing 34 MB and 275,011 lines in one day with a single player online — now Info, with `autoReload` letting it be raised live for debugging. (4) No archiving or retention — now capped at 64 MB per file with 14-day expiry. `throwExceptions` also turned off so a logging failure cannot surface as a server exception. Measured effect: one boot fell from 43,469 log lines to 1,320. | Resolved | High | 2026-08-30 |
| BUG-017 | Stat lookups allocated and double-computed on the swing path | `StatsInterface.GetTotalStat` emitted up to six interpolated trace strings per call and is reached from `CombatManager` several times per hit (83 call sites overall); on net48 an interpolated string is built at the call site whether or not the level is enabled, so this allocated unconditionally — an AGENTS.md rule 2 violation. Line 656 also called `GetStatLinearModifier` purely to log its result, then line 658 called it again for the real value, and each call re-entered a method that emitted four more trace strings. One 6-minute session with one player produced 9,589 stat trace lines. Fixed 2026-08-30: both methods gate every message behind `_logger.IsTraceEnabled` and the linear modifier is resolved once into a local. | Resolved | High | 2026-08-30 |
| BUG-018 | Ward assignment coverage is incomplete | The combat scalar is implemented, but most concrete location assignments remain unpopulated. Prototypes are reused at different levels, ranks, and ward tiers, so `08_move_creature_wards_to_spawns.sql` adds ward fields to world, instance, boss, and PQ spawns and reverses the 79 unsafe prototype changes from `07`. The attempted target-frame fix was disproved: `F_CREATE_MONSTER` offset 35 is the client difficulty mask (`1001`-`1004` display one to four skulls), not ward, and the 1.4.8 target tracker never populates its separate legacy `sigilEntryId`. Serialization now preserves difficulty unchanged. Script `18` corrects Bloodwrought Enclave and Bilerot Burrow to Lesser Ward and assigns Greater Ward to The Lost Vale. Other locations still require authoritative assignment. See `docs/WARD_SYSTEM.md`. | In Progress | High | 2026-08-31 |
| BUG-019 | Public quest or influence tracker missing on PQ entry | Rewards and progress worked, but the tracker initializer diverged from the official Spires packet sequence: its `F_OBJECTIVE_STATE` realm was zero, and the player `F_UPDATE_STATE` packet with state `0x12` was absent. Leaving a PQ also retained the old `PublicQuest` reference unless another PQ attached. The initializer now sends `F_OBJECTIVE_INFO`, realm-aware `F_OBJECTIVE_STATE`, `F_REALM_BONUS`, the missing player state, then `F_OBJECTIVE_UPDATE`; PQ boundaries detach stale state, refresh chapter influence, and resend the active PQ last. Live data identifies Spires as PQ `435`, PQ area `1`, within Dark Elf influence area `107` / influence `130`. Awaiting in-client confirmation. | In Progress | High | 2026-09-02 |
| BUG-020 | RvR area checks repeatedly emit flag and bolster messages | Duplicate bolster-removal callbacks could restore an already-restored level 40 battle rank; `ApplyBolster(0)` now returns when no bolster state remains, and the user confirmed the restoration spam is gone. The replacement unflag countdown message then repeated on area checks because the lake-exit path reset and announced the same active deadline each time. Countdown startup is now idempotent and both manual toggles and lake exits share it. Awaiting in-client confirmation of the unflag message. | In Progress | Medium | 2026-09-01 |
| BUG-021 | Ruinous Powers stage II and finale incomplete | The stage-II database rows were soldier substitutes rather than the 25 capture-verified `Perished Soul` objects. Script `10` restores their exact placements and three-second interactions; the user confirmed spawning, animation, and objective credit. The old finale also spawned Mathus and Kar'thok simultaneously under one objective. Script `11` separates the capture's 37-second “Wizard Lord Mathus” ritual from the final “Stage III,” moves Mathus to the ritual center, plays the captured transformation effect, and then spawns the Bloodhowler champion at the captured position. The capture contains no champion encounters during the tombstone interactions, so none were invented. Applied to the live database and Release build-verified; awaiting finale confirmation. | In Progress | High | 2026-09-01 |
| BUG-022 | Chapter influence remains stale across area changes | Official 1.4.8 area-close packets identify the zone that owned the previous tracker. Omitting that zone leaves stale RvR or PvE influence ahead of the new chapter until an RvR enter/leave cycle rebuilds the client's list. `SendChapterBar` now uses the capture-verified close layout for both tracker types. Script `12` also corrects Wulfsiege Forest from influence 66 to 67. Awaiting in-client confirmation. | In Progress | High | 2026-09-02 |
| BUG-023 | All world mailbox spawns missing | Script `02` restored the mailbox prototypes but the base database contained no corresponding `gameobject_spawns`. Script `13` restores 190 historical mailbox placements across 32 zones: 189 rows are identical in three ProjectWAR database snapshots and one additional Dragonwake row comes from the curated master integration. Extracted 1.4.8 client data independently identifies all six display IDs as the racial mailbox models. The live database contains all 190 exact rows after an idempotent reapplication and row-for-row verification. | Resolved | High | 2026-09-02 |
| BUG-024 | Bilerot Burrow entrance lands out of bounds | The shipped jump for entry `168899368` used local pins `(27131,25427,13313)` and heading `5000`, displacing the player into invalid geometry. Two independent official 1.4.8 captures initialize the player at exactly `(27213,25133,13421)` with heading `22`. Script `14` restores that destination and explicitly enforces the type-6 instance route. Script `18` supersedes its initial Greater Ward assignment with the correct Lesser Ward tier. Applied to the live database; awaiting in-client confirmation. | In Progress | High | 2026-09-02 |
| BUG-025 | Instance loads use invalid client coordinates | Instance destinations are stored as absolute world coordinates, while official `S_PLAYER_INITTED`, `F_CREATE_MONSTER`, and `F_CREATE_STATIC` captures place the player and objects in one shared 8192-unit client atlas slot. The instance updater and initial player placement are restored; a 2026-09-03 retest then confirmed terrain and creatures loaded, but the client displayed stale Necropolis of Zandri text. Instance initialization now retains its atlas transform for creature, pet, and static creation; transient zero-zone movement retains the authoritative instance zone; and `F_SWITCH_REGION` now writes the non-shared instance identifier at capture-verified offsets 6-7 instead of emitting the shared-world packet layout. The live entrance and nearest creature serialize to `(35405,229933)` and `(35668,229982)`, preserving their `(263,49)` separation. Awaiting in-client confirmation of the title correction. | In Progress | Critical | 2026-09-02 |
| BUG-026 | Bastion Stair base map incorrectly instanced | Zone `160` and all 18 arrivals into it were marked as a group instance, even though only boss encounter zones `163-166` should be instanced. Script `15` restores zone `160` and its arrivals to shared PvE routing, re-enables only the 195 world creature rows that exactly match the base-map instance population, and normalizes every boss-map arrival to type 6. Applied to the live database; awaiting in-client confirmation. | In Progress | High | 2026-09-02 |
| BUG-027 | Bilerot death release uses the dungeon respawn | The Destruction respawn for zone `196` pointed back inside Bilerot even though official 1.4.8 death captures switch to zone `161`. Script `17` preserves the zone-196 lookup row but sets `InZoneID = 161` and copies the canonical Inevitable City Destruction respawn coordinates from row `6`. Applied twice and verified against the live database; the server must reload its zone cache before testing. | In Progress | High | 2026-09-03 |
| BUG-028 | Beginner help tips never worked | Two defects. (1) `F_TOK_ENTRY_UPDATE` carries a trailing help-tip category byte and `TokInterface.SendTok` hardcoded it to `1`, so every Tome unlock popped an empty tip window. Disassembly of the 1.4.8 client's ToK handler (`0x4d2cb9`) confirms the layout: after the entry id it reads three bytes - unlocked flag, Tome announce flag, then the help-tip category - and any non-zero category calls the tip broadcaster at `0x5dd0aa`, which raises `HELP_TIP_UPDATED(entry, category)`. `EA_HelpTips` then resolves the title and body from the `HelpTipNames` / `HelpTipDescriptions` string tables with `entry - 11799`, so a chapter or area unlock produced an out-of-range index and a window with no text (no Lua error, matching the empty `ui_errors.log`). (2) Nothing granted the tips themselves: no row in `zone_areas.TokExploreEntry`, `chapter_infos.TokEntry` or `item_infos.TokUnlock` falls in 11800-11999, so no genuine tip could ever fire. Fixed 2026-09-03 with a `help_tips` trigger table (`19_restore_help_tips.sql`, 59 rows) bound to 22 server events - login, rank and renown rank, first XP/renown/money, death, party, warband, RvR flag and lake entry, scenario queue, quest completion, chapter and PQ entry, PQ bag, mailbox, NPC title, trade skill. `HelpTipService` rejects any row whose entry is not a named `tok_infos` section 101 record, which is the same set as the client's `data/gamedata/helptips.csv` (130 of 131 entries match). Tips unlock through the normal Tome path, so each is shown once per character; the Tome ticker is suppressed for them. No 1.4.8 source carries a per-tip category, so all rows ship as Beginner (1) in an editable column. Awaiting in-client confirmation. | In Progress | Medium | 2026-09-03 |
| BUG-029 | Equipping ward armour never awarded its fragment | The Tome sigil page listed "Equip Annihilator Helm" as an unfinished task while the helm was worn. Three causes. (1) `item_infos.TokUnlock3` was zero for every ward set except Invader, so no fragment unlock existed to grant. (2) Of the four equip paths in `ItemsInterface`, only one granted `TokUnlock3`; the direct-move path granted `TokUnlock` alone and the swap path granted nothing. Granting is now centralised in `EquipItem`, and `Player.OnLoad` calls `GrantEquippedItemUnlocks` after `TokInterface.Load` so items already worn are backfilled without re-equipping. (3) The fragment and the tasks that award it are separate unlocks. Tome section 5 encodes `Index` as the sigil tier and `Flag` as `fragment * 10 + task` in both the client's `unlockmapping.csv` and the server's `tok_infos`; `TokService.BuildWardTaskLookup` derives the pair from those columns and `AddTok` now awards the fragment whenever a task unlocks, so the task ticks and the ward counts. `20_restore_ward_fragment_equip_tasks.sql` sets `TokUnlock3` to the task entry for all ten armour sets (1,377 items, keyed on `TokUnlock2` plus `SlotId`, not names) and restores ten section 5 rows the world dump held as empty placeholders. Applied to the live database; all 99 section 5 tasks resolve to a fragment. Awaiting in-client confirmation. | In Progress | High | 2026-09-03 |
| BUG-030 | Ward sigil never appears on the enemy target frame | The sigil button, tooltip and Tome click-through in `targetunitframe.lua` are native and complete, but the 16-bit field they read is written only by the client's own constructor and clear-target routine, both storing zero. Nothing in the stock 1.4.8 client ever sets it, so no server packet can light the icon on an unmodified client; the Return of Reckoning client behaves the same. Server half, which is all that lives in this repository: `F_WARD_INFO` (`0xDF`), an opcode the stock client discards, sent by `Creature.SendWardInfo` after every `F_CREATE_MONSTER` as `(uint16 oid, byte tier)` -- including tier 0, because Oid reuse would otherwise leave a stale tier behind. Displaying it additionally requires a separately distributed client, maintained privately in WAR-RE-Toolkit and delivered through the launcher's patch manifest; an ordinary client simply ignores the packet. **Confirmed in game 2026-09-04:** Bilerot Burrow (`Ward = 1`) shows Lesser on all trash and bosses and The Lost Vale (`Ward = 2`) shows Greater, both immediately on target, with the Tome link opening the matching sigil page and nothing shown on unwarded creatures or world objects -- so the tier is real per-zone data rather than a constant. | Fixed | Medium | 2026-09-03 |
| BUG-031 | The Lost Vale cannot be entered from Avelorn, and no other instance can survive an orphaned boss row | Zoning in threw `NullReferenceException` at `Creature.cs:42` (`spawn.Proto.Name`) through `F_ZONEJUMP` -> `InstanceMgr.ZoneIn` -> `Create_new_instance` -> `Instance..ctor` -> `LoadBossSpawns`, so the instance was never created and the portal silently failed. The cause is a single boss row: `instance_boss_spawns` Entry 4275 (BossID 401, zone 260) has no `creature_protos` row, and `LoadBossSpawns` dereferenced the proto without checking -- `LoadSpawns` had guarded the same case since forever, `LoadBossSpawns` never did, so one bad row took down a whole dungeon. The portal data itself was fine (two `zone_jumps` rows for zone 260, Type 6, Enabled). Fixed in code, not data: `LoadBossSpawns` now logs and skips a spawn with a missing proto. That also unblocks zones 176 (6 orphan bosses, 1 orphan trash), 177 (2 and 1) and 179 (2). Entry 4275 is deliberately left in place -- it is a genuine 1.4.8 encounter slot (BossID 401 sits between Zaar the Painseeker 400 and Sechar 402) whose proto is missing from the world dump; deleting it would discard evidence and inventing a proto would be fabrication. Restore it when a source turns up. Two related defects fixed alongside: `Player.Teleport` created an instance only for zone Type 4, so teleporting into any Type 6 dungeon (Lost Vale, the city dungeons, 24 zones) dropped the player into an empty uninstanced map; and `ZoneIn` dereferenced the result of a `TryGetValue` on `instance_infos` without checking it. **Confirmed in game 2026-09-04:** the Avelorn portal (Portal Id 211812648) opened `Instance ID 1  Map: The Lost Vale`, with `Creature Proto not found boss spawn entry 4275 (bossId 401, zone 260) skipped` logged in the same millisecond -- the orphan is skipped and the dungeon loads. | Fixed | High | 2026-09-04 |

| BUG-032 | Ward fragments never awarded for tasks already completed | Reported in-session: equipping Annihilator, Sentinel and Invader gear ticked the Tome sigil task but never granted the ward. The data is correct — `20_restore_ward_fragment_equip_tasks.sql` is applied and all four sets carry 120 `TokUnlock3` rows resolving to real section 5 tasks (e.g. Annihilator chest `434149` -> `7629`, Index 1 Flag 51). The defect is `TokInterface.AddTok`: it opens with `if (HasTok(Entry)) return;` while the fragment cascade (`TryGetWardFragmentForTask`) sat at the *end* of the method. Any character who unlocked the equip task before the cascade existed — that is, every character wearing these sets — hit the early return, so the fragment was never granted and the ward stayed at zero despite a ticked task. The same early return silently defeated `ItmInterface.GrantEquippedItemUnlocks`, the login backfill added for BUG-029 specifically to repair this, because it grants through `AddTok`. `TokInterface.Load` could not repair it either: it calls `TrackWardFragment` per held tok, which only recognises fragment entries 7600-7624, and a task is not one. Fixed 2026-09-04: the ward task is resolved before the early return, so an already-held task still awards a missing fragment, and `TokInterface.BackfillWardFragments` (called from `Player.OnLoad` after the equip grants) repairs every held task including those completed by routes that leave nothing equipped. Strictly additive — fragments are permanent and are never recalculated from current equipment. Release build clean. This was found while diagnosing the reported symptom but was **not** its cause, which was BUG-033. The fresh-unlock path is confirmed in game 2026-09-04; the already-held repair branch and `BackfillWardFragments` are untested in client because no character is currently in that state — both are insurance against tasks completed before a future mapping change. | Resolved | High | 2026-09-04 |

| BUG-033 | Ward item migrations written to a table the server never reads | Equipping Annihilator, Sentinel or Invader gear never awarded a ward fragment, and the Tome sigil fragments stayed dark with no task ticked. The ward code was correct throughout; the data never reached it. `ItemService.LoadItem_Info` selects its source from `World.xml`: `UseMythicActionCoverageTables = true` (the shipped value) loads `mythic_src_item_infos`, false loads `item_infos`. Scripts `01`, `05` and `20` all wrote `TokUnlock3` to `item_infos` only, so on a default install `mythic_src_item_infos` had it populated on **0 of 88,727 rows** against 1,377 in `item_infos`. `Item_Info.TokUnlock3` was therefore 0 in memory for every item and `ItemsInterface.GrantEquipUnlocks` never fired its ward branch. `TokUnlock` was unaffected because that column is populated natively in both tables (2,888 rows), which is why ordinary set unlocks always worked and masked the split. Diagnosed by instrumenting the equip scan, which logged `Bigboy scanned 13 equipped item(s), 0 carrying a ward fragment task` while the database showed 7625/7627/7629 on those exact items. `21_sync_ward_fragment_tasks_to_mythic_items.sql` copies `TokUnlock3` across by `Entry`; the tables are entry-for-entry identical and already agreed exactly on `TokUnlock` and `TokUnlock2`, so nothing new is introduced. Applied and verified on the live database (1,377 = 1,377, 0 mismatches, idempotent on re-run). Item data is cached at boot, so the server must be restarted to pick it up. **Confirmed in game 2026-09-04:** after the restart the equip scan reports `Bigboy scanned 13 equipped item(s), 5 carrying a ward fragment task` where it previously reported 0, the character holds all ten expected section 5 rows (five tasks plus the five fragments they award), and the Tome sigil wedges light on login — Lesser 3/5 from Annihilator boots, shoulders and chest, Greater 2/5 from Sentinel gloves and helm. | Fixed | High | 2026-09-04 |

| BUG-034 | Bloodlord armor has no set linkage for 19 of 24 careers | Found 2026-09-04 while testing ward task 3: equipping `Bloodlord Espaliers` awarded its ward fragment correctly but the item showed no set membership. `mythic_src_item_sets` defines **5** Bloodlord sets against **24** for every other ward set (Annihilator, Conqueror, Invader, Sentinel each have one per career), and only 25 of 120 Bloodlord armor pieces carry a non-zero `ItemSet`. The five that exist resolve and are named normally (`Bloodlord War Boyz' Kit`, `Bloodlord Squig Calla's Kit`, ...), all Destruction. The ward data is unaffected — `Bloodlord Espaliers` has the correct `TokUnlock3 = 7677` (Lesser task 3, shoulders) — so this is set-bonus data, not ward data. Rarity is **not** anomalous: Bloodlord's mix of 3 and 4 matches Annihilator exactly, so the purple colour is correct and only the linkage is missing. Closing this needs the authentic per-career Bloodlord set names and bonuses from a source of truth; the 19 missing sets must not be synthesised from the five that exist or copied from another set's bonuses. Note `item_sets` is empty (0 rows) while `mythic_src_item_sets` holds all 923, so set data exists only for `UseMythicActionCoverageTables = true` — see BUG-033 for the same dual-table split. | Open | Medium | 2026-09-04 |

| BUG-035 | ~~Literal `^M` suffix corrupting 5,210 world names~~ **NOT A BUG — retracted** | **Retracted 2026-09-04, same day.** The `^M`/`^m` suffix is a **grammatical gender marker for localisation**, not corruption. `creature_protos` also carries `^f` (756), `^n` (157) and `^p` (2) — masculine, feminine, neuter, plural — and the client uses the same convention in `data/strings/english/zones/zone060_area_names.txt`, where `Mad Mixas^n,in` pairs a neuter marker with a grammar hint. German and French localisation needs gender agreement, which is what these encode. The original diagnosis fixated on `^M` alone because that is also how caret notation renders a carriage return, and never checked whether other caret suffixes existed — they do, and they are the disproof. `22_strip_caret_m_from_names.sql` was written, applied to the live database, committed and pushed before this was caught; it has been reverted by restoring `Name` from `Database/war_world.7z` for all three tables, verified byte-identical to the base dump (0 differences under `BINARY` comparison), and the script has been deleted. **Do not strip caret suffixes from name columns.** The genuine finding underneath is unchanged: name-based lookup must tolerate the marker, so the ward task 4 boss resolution needs to match on the name with its suffix rather than assume a bare name. Original (incorrect) report retained below for the record. | Retracted | — | 2026-09-04 |
| ~~BUG-035 (original text)~~ | Literal `^M` suffix corrupting 5,210 world names | Found 2026-09-04 while resolving the ward task 4 boss for `Kill Lord Slaurith 5 Times`: the prototype was stored as `Lord Slaurith^M`, 15 characters ending hex `68 5E 4D` — the two ordinary ASCII characters `^` and `M`, **not** a carriage return (0x0D). A CR was text-substituted into its caret notation during a CSV import, so it is in the data and cannot be removed by trimming control characters. Scope: `creature_protos.Name` 2,954 rows (1,674 `^M` / 1,280 `^m`), `quests_maps.Name` 2,240, `boss_spawn.Name` 16. Every occurrence is a suffix and none mid-string in all three tables, so a two-character strip is exact. **2,441 of the 9,795 spawned prototypes are affected**, including very common NPCs — `Hare^m` (445 spawns), `Raven^m` (270), `Bloody Sun War Blasta^m` (267) — so it is player-visible on nameplates, not merely a database blemish. It also breaks every name-based lookup, which is how it surfaced: exact-match resolution of the eight ward task 4 bosses found 2 of 8 before and 6 of 8 after. `22_strip_caret_m_from_names.sql` strips the suffix in all three tables. Applied and verified on the live database (0 remaining, idempotent on re-run, `Lord Slaurith` resolves). Server restart needed for the creature cache. Remaining unmatched ward bosses (`Warlock Peenk`, `Skull Lord`, `Dralel the Whitefire Matron`) are a separate naming question, not this corruption. | Fixed | Medium | 2026-09-04 |

| BUG-036 | Dungeon public quests paid Order players Destruction influence | Found 2026-09-04 while restoring Bastion Stair. `zone_areas` holds the influence track per realm — zone 160 is `OrderInfluenceId 129` / `DestroInfluenceId 128`, Mount Gunbad `64`/`65` — and both dungeons' PQs store the **Destruction** id in `pquest_info.ChapterId` (128 for all ten Bastion PQs, 65 for all nine Gunbad PQs). Despite the column name that is not a chapter reference: `chapter_infos` 128 is "Chapter 20: Surprise Attack" in zone 9 and 65 does not exist, while the real dungeon chapters are 2/6 and 1/5. `PublicQuest` picked the influence id per realm from `CurrentArea` on the objective tick, but the two awards that carry the weight — 250 on stage completion (`PublicQuest.cs:723`) and 500 on PQ completion (`:771`) — used `Info.ChapterId` unconditionally. So an Order player running either dungeon earned Destruction influence, their own bar never filled, and the influence gate on the wing bosses could never open for Order. Fixed by routing all three sites through one realm-aware `GetInfluenceId(Player)` helper that prefers the area's per-realm id and falls back to `ChapterId`. The original `Info.Type == 0` guard is preserved, which confines the change to these two dungeons — all 19 of their PQs are Type 0, while the other 557 world PQs are Type 1/2/3 and keep their existing behaviour. Release build clean. Awaiting in-client confirmation that an Order character gains Bastion Stair influence from a PQ. | In Progress | High | 2026-09-04 |

## Not Bugs (investigated and explained)

| Topic | Finding |
|:---|:---|
| Land of the Dead flight master unreachable | Working as designed. `LotdService.CanRealmAccessLotd` requires the tracker to be `Paused` **with** a non-neutral owning realm; the default state is `Active`/Neutral at 0 of 500 points, earned 100 per T4 battlefront lock. So the expedition opens only after one realm wins the race, then stays open for `UnlockDurationMinutes` (30). Zone 191's taxi rows are healthy for both realms. This is indistinguishable in-game from a broken flight master, and there was previously no way to control it — hence the `.lotd` GM commands added 2026-08-30 (`status`, `unlock`, `reset`, `award`). |
| "Slow SQL" warnings at startup | Not a scaling problem. The ~2.1s query is the one-time `Item_Info` load of 88,727 rows into memory (~24µs/row) so that gameplay reads the cache rather than the database. It does not grow with player count. The genuine scaling issue was unindexed per-login lookups — see `Database/03_add_hot_path_indexes.sql`. |
| 36 public quests reported as skipped (formerly BUG-012) | The keep and fortress rows are intentionally not registered as ordinary cell-spawned `PublicQuest` objects. `LoadPublicQuests` reserves every `PQuestId` referenced by `keep_infos`, and startup then attaches the same `PQuest_Info` records to their keeps for objective packet IDs and reward-chest coordinates. All 36 keep references resolve. The old `Skipped PQs` log label was misleading and now reports that the keep-owned metadata was prepared and excluded from normal cell spawning. |
| Mythic graph reports zero component links and requirements (formerly BUG-014) | This is an unfinished parity workstream, not a failed runtime join. The selected toolkit dumps (`Londos Server v2`) contain empty `AbilityComponentXComponent` and `AbilityRequirmentBin` tables, so the imported zero counts are accurate. The translator currently parses embedded `MythicComponentData` and only reports the separate link/requirement collections; it never consumes them. The toolkit's parallel `Abilities` dataset has 29,633 compatible link rows and 651 requirement rows (all link IDs and all 558 referenced requirement IDs match the imported graph), but using those relationships requires a deliberate next-phase translator/importer design. `skipped_unknown_entries` is also an intentional filter against the runtime `abilities` source set. Track remaining semantic coverage under BUG-007 rather than treating these counters as a lookup defect. |

## Bug Reporting Guidelines

When adding a new bug, please include:
- **Summary**: A concise title.
- **Description**: Detailed steps to reproduce and expected vs. actual behavior.
- **Priority**: Low, Medium, High, or Critical.
- **Status**: Open, In Progress, Resolved, or Closed.
