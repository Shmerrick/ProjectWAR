<UserControl x:Class="AbilityTool.Client.Ui.UpdateAbsCommandUC"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      x:Name="AbilityCommandsPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Width="800">
            <Label>Choose an target:</Label>
            <ComboBox x:Name="targets" DataContext="{Binding Path=targets}"/>
            <Label>Choose an effectSource:</Label>
            <ComboBox x:Name="effectSources" DataContext="{Binding Path=effectSources}"/>
        </StackPanel>
    </Grid>
</UserControl>
